[build]
builder = "nixpacks"
buildCommand = "cd backend && cargo build --release"

[deploy]
startCommand = "cd backend && FRONTEND_PATH=../frontend ./target/release/kitso_portfolio_backend"
workingDir = "/app"
